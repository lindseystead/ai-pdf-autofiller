# Demo session transcript

Generated by `scripts/generate_demo_output.py`. Use this in launch posts and README until a screen recording is added.

```
$ git clone https://github.com/lindseystead/ai-pdf-autofiller.git
$ cd ai-pdf-autofiller && make run-api
INFO:     Uvicorn running on http://0.0.0.0:8000

$ open http://localhost:8000/playground
# Upload samples/sample_form.pdf, paste JSON, click Fill PDF → Download

$ curl -s -X POST http://localhost:8000/fill \
  -F "pdf_file=@samples/sample_form.pdf;type=application/pdf" \
  -F 'user_data={"firstname":"Jane","lastname":"Doe","dob":"1990-01-01"}' \
  -F "strict=true" \
  -o filled.pdf

$ python -c "from pdf_autofiller import fill; fill('samples/sample_form.pdf', {'firstname':'Jane'}, 'filled.pdf')"
```

**Playground screenshot:** Open `/playground` after `make run-api` — drag a PDF, paste JSON, download the result in under 30 seconds.
