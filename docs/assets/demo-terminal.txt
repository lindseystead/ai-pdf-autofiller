$ make run-api
INFO:     Uvicorn running on http://0.0.0.0:8000

$ curl -s http://localhost:8000/health
{
  "status": "ok",
  "service": "pdf-autofiller",
  "version": "0.4.3",
  "checks": {
    "auth": "disabled",
    "alias_directory": ".../form_aliases",
    "alias_pack_count": "2"
  }
}

$ open http://localhost:8000/playground
# Upload samples/sample_form.pdf → Fill PDF → download filled.pdf

$ curl -s -X POST http://localhost:8000/fill \
  -F "pdf_file=@samples/sample_form.pdf;type=application/pdf" \
  -F 'user_data={"firstname":"Jane","lastname":"Doe","dob":"1990-01-01"}' \
  -F "strict=true" \
  -o filled.pdf
$ file filled.pdf
filled.pdf: PDF document, version 1.3, 1 pages

$ python -c "from pdf_autofiller import fill; fill('samples/sample_form.pdf', {'firstname':'Jane','lastname':'Doe','dob':'1990-01-01'}, 'sdk-filled.pdf')"
# Local SDK pipeline (no server) writes txtFirstName=Jane, txtLastName=Doe, txtDOB=1990-01-01

$ PYTHONPATH=src python -m scripts.demo_workflow samples/sample_form.pdf \
  '{"firstname":"Jane","lastname":"Doe","dob":"1990-01-01"}'
============================================================
Running PDF Autofiller Workflow
============================================================
✓ PDF loaded successfully — 5 form fields
✓ Mapping complete — 3 decisions, 0 missing required
✓ PDF filled successfully → samples/sample_form_filled.pdf
